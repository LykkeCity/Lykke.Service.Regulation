using Microsoft.AspNetCore.Mvc;

namespace Lykke.Service.Regulation.Controllers
{
    public class ClientRegulationController : Controller
    {
        /*
        Task<IEnumerable<string>> GetAvailableRegulation(string clientId);
        Получить идешники доступных регуляций на клиенте

        Task AddAvailableRegulation(string clientId, string regulationId);
        добавить клиенту доступную регуляцию, если уже добавлена регуляция, то не чего не делать, 
        т.е. дублирования не должно быть

        Task SetRegulation(string clientId, string regulationId);
        установить регуляцию клиенту.
        Если у клиента регуляция не доступна, то должна быть ошибка


        Task<string> GetRegulation(string clientId);
        получить текущую регуляцию клиента
        Если клиент не найден, то вернуть null наверно, потом подумаем что делать в этом случае.

        */
    }
}
